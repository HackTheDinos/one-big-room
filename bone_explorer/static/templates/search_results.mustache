{{#results_list}}
  <div class="row top-buffer-lg">
    <div class="col-md-4">
      <a href="{{url}}" class="thumbnail" style="margin:5px">
        <img src="{{imageUrl}}" alt="" />
      </a>
      <a href="{{url}}" class="thumbnail results" style="margin:5px" >
        {{>slice_preview}}      
      </a>
    </div>

    <div class="col-md-8">
        <h3>
          <a href="{{url}}">{{title}}</a>
          <small>{{vernacular_name}}</small>
        </h3>
        {{>classification}}
    </div>
 </div>
{{/results_list}}
